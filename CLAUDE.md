# maale-css — הוראות לסשן

זה ריפו העיצוב של חנות מעלה המשלוחים (Hyperzod). **לפני כל עבודה קרא את `DESIGN_METHOD.md`** — שם המפה, הכלים, אטלס ה-DOM, המלכודות ושפת העיצוב.

כללי ברזל (של דוד, בעל העסק):
1. לפני שינוי — קוראים מה כבר כתוב בריפו על אותו רכיב (`grep -n` ב-`global-cdn.css` ו-`custom-footer.js`) ומחליפים; לא שכבה על שכבה.
2. כל שינוי נבדק ב-Chrome ללא ראש על מוק מה-HTML האמיתי (`tools/`), מובייל + דסקטופ, לפני push. אחרי push — אימות חי.
3. סקופ הדוק (`html body #עוגן …`), בדיקת בקרה מחוץ לסקופ. לא נוגעים בבורר רבעי הפיצה (`mhq-`).
4. `node --check custom-footer.js` לפני commit. commit עם `-c user.name=davidebug10 -c user.email=davidpoho10@gmail.com`.
5. עניינים של כסף (צ'ק-אאוט/תשלום) — אפס טעויות; שינוי התנהגות דורש אישור של דוד.
6. דיווח בעברית עסקית עם צילום מהאתר החי; לומר מה לא אומת.
